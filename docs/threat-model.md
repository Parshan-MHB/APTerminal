# Threat Model

The standalone threat-model document has been merged into [technical-spec.md](technical-spec.md).

Use the following sections in the handbook as the current security source of truth:

- `Security Model`
- `Permissions`
- `Release Readiness`
- `Known Limitations`
- `Troubleshooting`

Use [connection-security-diagram.md](connection-security-diagram.md) for the implemented LAN, private-overlay, pairing, reconnect, and preview-authorization flow.

This file remains only as a compatibility pointer because older repo instructions and plan references still mention `docs/threat-model.md`.
